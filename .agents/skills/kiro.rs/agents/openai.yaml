interface:
  display_name: "Kiro.rs"
  short_description: "Repo-specific patterns and workflows for kiro.rs"
  default_prompt: "Use the kiro.rs repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true